# 部署指南 · 让系统 24 小时在线 + 手机随时可用

本系统有三种使用形态，按需选择（可以叠加）：

| 形态 | 电脑关机后能用吗 | 手机能用吗 | 需要什么 |
|---|---|---|---|
| ① 纯本机离线 | ❌ | ❌ | 双击 `index.html` 即可（现状，零门槛） |
| ② 家里电脑当局域网服务器 | ❌（服务器电脑需开机） | ✅（同一 WiFi 下） | 电脑运行 `启动同步服务` 脚本 |
| ③ 云端部署（推荐） | ✅ | ✅（任何网络，含 4G/5G） | 免费云主机 + 10 分钟部署 |

形态③就是"电脑关机时也能运行"的答案：把系统放到云端的免费主机上，
网址 24 小时可访问，手机和电脑打开同一网址，数据自动双向同步。

---

## 一、云端部署（推荐，免费，约 10 分钟）

以 Render.com 为例（免费主机，无需信用卡，支持中国大陆访问）：

### 第 1 步：注册
1. 浏览器打开 `https://render.com`，点右上角 **Get Started**。
2. 用邮箱或 Google / GitHub 账号注册并登录。

### 第 2 步：上传系统文件到 GitHub
> Render 从 GitHub 仓库拉取代码，所以先把文件传上去。

1. 打开 `https://github.com`，注册/登录，点 **New repository**。
2. 仓库名随意（如 `my-stock-system`），选 **Private**（私有，更安全），点 **Create repository**。
3. 点 **uploading an existing file** 链接，把 `stock-system` 文件夹里的**全部文件**拖进去
   （`index.html`、`server.js`、`style.css`、`js/` 文件夹、`icons/` 文件夹、`manifest.json`、`service-worker.js` 等，
   `README.md` 和本指南可选）。
   注意：**不要上传 `cloud-data.json`**（那是你本机的数据）。
4. 点 **Commit changes** 提交。

### 第 3 步：在 Render 创建服务
1. 回到 Render 控制台，点 **New +** → **Web Service**。
2. 选择刚创建的仓库（首次需点 **Connect GitHub account** 授权）。
3. 关键配置：
   - **Name**：随便填，会出现在网址里（如 `my-stock` → 网址是 `https://my-stock.onrender.com`）
   - **Region**：选 Singapore 或 Hong Kong（离国内近）
   - **Branch**：`main`
   - **Runtime**：Node
   - **Build Command**：留空（什么都不用填）
   - **Start Command**：`node server.js`
   - **Instance Type**：**Free**
4. 点 **Advanced** → **Add Environment Variable**，添加一个变量（这就是你的访问密码）：
   - Key：`TOKEN`
   - Value：自己想一个长一点的密码（如 `MyStock2026!abc`）
5. 点 **Create Web Service**，等待 1-2 分钟构建完成。

### 第 4 步：在系统里绑定
1. 浏览器打开 `https://你的名字.onrender.com`，能看到系统页面即部署成功。
2. 进入 **设置 → 云端同步**，服务器地址填 `https://你的名字.onrender.com`，
   访问令牌填刚才的 `TOKEN` 密码，点 **测试连接**，再点 **保存并立即同步**。
3. 手机浏览器打开同一网址 → **添加到主屏幕**，完成！

之后的使用习惯：
- 电脑（开不开机都行）和手机打开同一网址，数据自动同步；
- 没网时照常记录，恢复联网后自动补传；
- 头部徽章显示同步状态（绿点 = 已同步）。

> **免费版说明**：Render 免费实例 15 分钟无人访问会休眠，下次打开需等约 30-60 秒唤醒。
> 对个人记账复盘完全够用；若介意，可升级付费实例或改用 Railway / Fly.io。

### 备选云平台
| 平台 | 免费额度 | 特点 |
|---|---|---|
| Render.com | 512MB 内存 | 本文示例，最简单 |
| Railway.app | 每月 $5 试用额度 | 界面友好，不休眠 |
| Fly.io | 3 个小虚拟机 | 需命令行操作 |
| 家里的旧电脑 / 树莓派 | 免费 | 需保持开机 + 端口映射，见下节 |

---

## 二、家里电脑当局域网服务器（手机同 WiFi 使用）

适合"电脑和手机都在家"的场景，不依赖任何云服务：

1. 电脑安装 Node.js：打开 `https://nodejs.org` → 下载 LTS 版 → 一路下一步安装。
2. 双击文件夹里的：
   - Windows：`启动同步服务-Windows.bat`
   - macOS：`启动同步服务-macOS.command`
   - Linux：终端执行 `bash 启动同步服务-Linux.sh`
3. 窗口会显示两个地址：
   - `本机访问: http://localhost:8000` —— 电脑浏览器用
   - `局域网访问: http://192.168.x.x:8000` —— **手机连同一 WiFi 后用这个**
4. 手机浏览器打开局域网地址 → 添加到主屏幕即可。
5. 在「设置 → 云端同步」里填 `http://192.168.x.x:8000`（令牌留空）即可在手机与电脑间同步。

> 提示：此模式下电脑就是服务器，关机后手机无法访问——想关机也能用请用方案一（云端部署）。
> 家里路由器 IP 一般固定，若手机突然连不上，重跑一次脚本看 IP 是否变化。

---

## 三、手机「添加到主屏幕」说明

**iPhone（iOS Safari）**
1. Safari 打开系统网址（云端地址或局域网地址）。
2. 点底部「分享」按钮（方框加向上箭头）。
3. 滚动找到「添加到主屏幕」→ 确定。
4. 桌面出现"交易系统"图标，点开即全屏使用，与原生 App 体验一致。

**Android（Chrome）**
1. Chrome 打开系统网址。
2. 点右上角「⋮」菜单 → 「添加到主屏幕」或「安装应用」。

添加后即使**完全断网**也能打开应用（页面已缓存），可正常记录交易、复盘、查询评分，
联网后数据自动同步。

---

## 四、数据与安全须知

1. **令牌即密码**：云端部署务必设置 `TOKEN` 环境变量，且不要用简单密码——
   它是你数据的唯一防线。局域网自用可留空。
2. **数据归属**：云端保存的是 `cloud-data.json` 一个文件，随时可在
   「设置 → 备份与恢复」导出 JSON 留底。建议每周导出一次。
3. **多设备同时离线编辑**：按"每条记录最新者优先"自动合并；删除操作请尽量在联网状态下进行。
4. **AI 问询（Ollama）在手机上的说明**：Ollama 跑在你自己电脑上。手机与电脑连同一 WiFi、
   且电脑 Ollama 以 `OLLAMA_HOST=0.0.0.0` 启动（一键脚本已默认配置）时，把「设置 → 本地 AI」
   的地址改为 `http://电脑IP:11434` 即可在手机上使用 AI 问询；在外网（4G/5G）则不可用，
   此时逐条勾选的规则评分依然完整可用。
5. **换电脑迁移**：新设备打开网址 → 设置里填同样的服务器地址和令牌 → 数据自动同步下来。

---

## 五、常见问题

**Q：云部署后旧电脑上的数据怎么搬过去？**
A：旧电脑打开系统 →「设置 → 云端同步」填云端地址和令牌 → 保存并立即同步，
本地数据会自动推送到云端（两台设备都有数据时自动按记录合并，不会丢）。

**Q：同步徽章一直显示"连接失败"？**
A：① 核对地址拼写；② Render 免费实例可能在休眠唤醒中，等一分钟重试；
③ 令牌是否与服务端 `TOKEN` 完全一致（区分大小写）。

**Q：想把云端数据清空重来？**
A：登录 Render → 服务 → 环境 → 直接删除服务重建；或本地「设置 → 清空全部数据」后立即同步。
清空前记得先导出备份！
